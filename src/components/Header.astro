---
import { SITE_TITLE } from '../consts';
import { getUi, localeBase } from '../i18n/ui';
import HeaderLink from './HeaderLink.astro';
const lang = Astro.currentLocale;
const base = localeBase(lang);
const t = getUi(lang);

// 计算切换链接的路径
const pathname = Astro.url.pathname;
const zhPath = lang === 'zh' ? pathname : `/zh${pathname === '/' ? '' : pathname}`;
const enPath = lang === 'en' ? pathname : pathname.replace(/^\/zh/, '') || '/';
---

<header>
	<nav>
		<h2><a href={base || '/'}>{SITE_TITLE}</a></h2>
		<div class="internal-links">
			<HeaderLink href={base || '/'}>{t.nav.home}</HeaderLink>
			<HeaderLink href={`${base}/sticko`}>{t.nav.sticko}</HeaderLink>
		</div>
		<div class="right-section">
			<div class="lang-dropdown">
				<button class="lang-btn">
					<svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"></circle><line x1="2" y1="12" x2="22" y2="12"></line><path d="M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z"></path></svg>
					<span>{lang === 'zh' ? '中文' : 'English'}</span>
				</button>
				<div class="dropdown-content">
					<div class="dropdown-content-inner">
						<a href={enPath} class={lang === 'en' ? 'active' : ''}>English</a>
						<a href={zhPath} class={lang === 'zh' ? 'active' : ''}>中文</a>
					</div>
				</div>
			</div>
			<div class="social-links">
			<a href="https://x.com/jovidalao" target="_blank">
				<span class="sr-only">Follow jovidalao on X</span>
				<svg viewBox="0 0 16 16" aria-hidden="true" width="32" height="32"
					><path
						fill="currentColor"
						d="M12.6.75h2.454l-5.36 6.142L16 15.25h-4.937l-3.867-5.07-4.425 5.07H.316l5.733-6.57L0 .75h5.063l3.495 4.633L12.6.75Zm-.86 13.028h1.36L4.323 2.145H2.865l8.875 11.633Z"
					></path></svg
				>
			</a>
			<a href="https://github.com/jovidalao" target="_blank">
				<span class="sr-only">Go to Astro's GitHub repo</span>
				<svg viewBox="0 0 16 16" aria-hidden="true" width="32" height="32"
					><path
						fill="currentColor"
						d="M8 0C3.58 0 0 3.58 0 8c0 3.54 2.29 6.53 5.47 7.59.4.07.55-.17.55-.38 0-.19-.01-.82-.01-1.49-2.01.37-2.53-.49-2.69-.94-.09-.23-.48-.94-.82-1.13-.28-.15-.68-.52-.01-.53.63-.01 1.08.58 1.23.82.72 1.21 1.87.87 2.33.66.07-.52.28-.87.51-1.07-1.78-.2-3.64-.89-3.64-3.95 0-.87.31-1.59.82-2.15-.08-.2-.36-1.02.08-2.12 0 0 .67-.21 2.2.82.64-.18 1.32-.27 2-.27.68 0 1.36.09 2 .27 1.53-1.04 2.2-.82 2.2-.82.44 1.1.16 1.92.08 2.12.51.56.82 1.27.82 2.15 0 3.07-1.87 3.75-3.65 3.95.29.25.54.73.54 1.48 0 1.07-.01 1.93-.01 2.2 0 .21.15.46.55.38A8.012 8.012 0 0 0 16 8c0-4.42-3.58-8-8-8z"
					></path></svg
				>
			</a>
		</div>
	</div>
	</nav>
</header>
<style>
	header {
		margin: 0;
		padding: 0 1em;
		background: white;
		box-shadow: 0 2px 8px rgba(var(--black), 5%);
	}
	h2 {
		margin: 0;
		font-size: 1em;
	}

	h2 a,
	h2 a.active {
		text-decoration: none;
	}
	nav {
		display: grid;
		grid-template-columns: 1fr auto 1fr;
		align-items: center;
	}
	.internal-links {
		display: flex;
		justify-content: center;
	}
	nav a {
		padding: 1em 0.5em;
		color: var(--black);
		border-bottom: 4px solid transparent;
		text-decoration: none;
	}
	nav a.active {
		text-decoration: none;
		border-bottom-color: var(--accent);
	}
	.lang-dropdown {
		position: relative;
		display: inline-block;
	}
	.lang-btn {
		display: flex;
		align-items: center;
		gap: 0.5em;
		background: none;
		border: 1px solid #ddd;
		padding: 0.4em 0.8em;
		border-radius: 20px;
		cursor: pointer;
		font-size: 0.9em;
		color: var(--black);
		transition: all 0.2s;
	}
	.lang-btn:hover {
		border-color: var(--accent);
		color: var(--accent);
	}
	.dropdown-content {
		display: none;
		position: absolute;
		right: 0;
		top: 100%;
		background-color: transparent; /* Transparent parent to hold the gap */
		min-width: 140px;
		z-index: 100;
		padding-top: 10px; /* This is the clickable 'bridge' gap */
	}
	.dropdown-content-inner {
		background-color: white;
		box-shadow: 0px 8px 16px 0px rgba(0,0,0,0.1);
		border-radius: 8px;
		overflow: hidden;
		border: 1px solid #eee;
	}
	/* Remove the old bridge */
	.lang-dropdown:hover .dropdown-content {
		display: block;
	}
	.dropdown-content a {
		color: var(--black);
		padding: 10px 16px;
		text-decoration: none;
		display: block;
		font-size: 0.9em;
		text-align: left;
		border-bottom: none !important;
	}
	.dropdown-content a:hover {
		background-color: #f9f9f9;
		color: var(--accent);
	}
	.dropdown-content a.active {
		font-weight: bold;
		color: var(--accent);
		background-color: #f0f7ff;
	}
	.right-section {
		display: flex;
		align-items: center;
		gap: 1em;
		justify-self: end;
	}
	.social-links,
	.social-links a {
		display: flex;
	}
	@media (max-width: 720px) {
		.social-links {
			display: none;
		}
	}
</style>
